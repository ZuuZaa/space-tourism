import { PageLayout } from "@/components/page-layout";
import { NAV_LINKS } from "@/config/constans";

const Destination = () => {
  return <PageLayout pageDetails={NAV_LINKS.DESTINATION}/>;
};

export default Destination;